#include<iostream>
using namespace std;
int main()
{
    int x, y, space;
    cout<<"Masukkan nilai N: ";
    cin>>y;

    for (int p = y; p > 0; p--){
        for(int x = y; x > 0; x-- ){
            cout<<"l";
        }

        cout<<endl;

    }
return 0;
}